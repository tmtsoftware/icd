package csw.services.icd.model

import com.typesafe.config.Config

/**
 * Models a value that is based on the json-schema.
 * This can define a primitive type, enum, array, or object, for example.
 */
case class JsonSchemaModel(config: Config) {

}
