subscribe {
  telemetry = [
    {
      subsystem = WFOS
      name = blue.filter
      requiredRate = 0
      maxRate = 10
    }
    {
      subsystem = TCS
      name = elevation
      requiredRate = 10
      maxRate = 10
    }
  ]

  health = [
    {
      subsystem = WFOS
      name = blue.filter
      requiredRate = 0
      maxRate = 10
    }
  ]
}
