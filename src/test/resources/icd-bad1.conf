publish {
  telemetry = [
    // A telemetry item should be the same as a configuration set of args I think.  Would be nice to reuse somehow?
    // But has some extra data
    {
      name = "status1"
      rate = 0
      maxRate = 100
      archive = Yes
      archiveRate = 10
      attributes = [
        {name = a1, desc = "description - single value with min/max", valueType = int, minValue = -2, maxValue = 22, size = 1, units = m}
        {name = a2, desc = "description - array of float", valueType = float, size = 5, units = mm}
        {name = a3, desc = "description -- enum choice", valueType = enum, default = green, valueList = "green|red|blue"}
      ]
    }
    {
      name = "status2"
      rate = 10
      maxRate = 10
      archive = Nope
    }
  ]

  events = [
    {
      myKey = myValue
    }
  ]
}
