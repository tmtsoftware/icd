subsystem = WFOS
component = filter
subscribe {
  description = "Optional overview of subscriptions"

  telemetry = [
    {
      subsystem = WFOS
      component = pointing
      name = blue.filter
      requiredRate = 0
      maxRate = 10
    }
    {
      subsystem = TCS
      component = tcsPk
      name = elevation
      requiredRate = 10
      maxRate = 10
    }
  ]


  events = [
    {
      subsystem = WFOS
      component = pointing
      name = blue.filter
      requiredRate = 0
      maxRate = 10
    }
    {
      subsystem = TCS
      component = tcsPk
      name = elevation
      requiredRate = 10
      maxRate = 10
    }
  ]

  eventStreams = [
    {
      subsystem = WFOS
      component = pointing
      name = blue.filter
      requiredRate = 0
      maxRate = 10
    }
    {
      subsystem = TCS
      component = tcsPk
      name = elevation
      requiredRate = 10
      maxRate = 10
    }
  ]


  alarms = [
    {
      subsystem = WFOS
      component = pointing
      name = blue.filter
      requiredRate = 0
      maxRate = 10
    }
    {
      subsystem = TCS
      component = tcsPk
      name = elevation
      requiredRate = 10
      maxRate = 10
    }
  ]

}
