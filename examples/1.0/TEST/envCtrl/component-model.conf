subsystem = TEST
component = envCtrl

title = "Environmental Control Assembly (EnvCtrl)"
prefix = test.ncc.envCtrl
componentType = Assembly

description = """
This describes the interface to the Environmental Control Assembly *(EnvCtrl)*
"""

modelVersion = "1.0"  // version of model in use for component
wbsId = TMT.AO.TEST
