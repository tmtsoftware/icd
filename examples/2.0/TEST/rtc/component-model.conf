subsystem = TEST
component = rtc

prefix = test.rtc
modelVersion = "2.0"
title = "RTC Assembly"
wbsId = TMT.AO.TEST

componentType = Assembly

description = """
This is the interface of the TEST RTC Assembly.
"""
