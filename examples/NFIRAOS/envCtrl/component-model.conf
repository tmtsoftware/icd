subsystem = NFIRAOS
component = envCtrl

title = "Environmental Control Assembly (EnvCtrl)"
prefix = nfiraos.ncc.envCtrl
componentType = Assembly

description = """
This describes the interface to the __Environmental Control Assembly__ *(EnvCtrl)*
"""

modelVersion = "1.0"  // version of model in use for component
wbsId = TMT.AO.NFIRAOS
