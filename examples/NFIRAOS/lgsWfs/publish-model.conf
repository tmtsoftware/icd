publish {

  telemetry = [
    {
      name = "configured"
      description = "LGS WFS configuration state"
      archive = false
      attributes = [
        {
          name = stateOverall
          description = "LGS WFS configuration state"
          type = boolean
        }
      ]
    }

    // XXX TBD

  ]
}
