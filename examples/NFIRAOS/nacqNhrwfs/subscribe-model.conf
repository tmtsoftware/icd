subsystem = NFIRAOS
component = nacqNhrwfs

subscribe {
  telemetry = [
    {
      subsystem = TCS
      component = tcsPk
      name = zenithAngle
      requiredRate = 10
    }
    {
      subsystem = TCS
      component = tcsPk
      name = parallacticAngle
      requiredRate = 1
      maxRate = 1
    }
  ]

}
